# ---------------------------------------------------------------------------
# Folia API Maven coordinate version.
#
# UNCONFIRMED. Nobody has verified which Folia build actually targets
# Minecraft 26.2 yet (26.2 is the pinned target for the whole Oxide project;
# see docs/ARCHITECTURE.md). The value below is a PLACEHOLDER — it will not
# resolve against any real Maven repository. This module will NOT build
# until you replace it with a real, verified `dev.folia:folia-api` version
# for MC 26.2. Check before trusting anything:
#   https://repo.papermc.io/service/rest/repository/browse/maven-public/dev/folia/folia-api/
#   https://papermc.io/downloads/folia
foliaApiVersion=UNCONFIRMED-SET-ME-TO-REAL-26.2-FOLIA-API-VERSION

# paper-plugin.yml `api-version`. Pinned to the project's target MC version
# per docs/ARCHITECTURE.md. Not independently re-verified here — same
# caveat as foliaApiVersion above applies to whether 26.2 is buildable yet.
mcApiVersion=26.2

javaVersion=21
pluginVersion=0.1.0-SNAPSHOT
