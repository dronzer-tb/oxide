/target
**/target
**/*.rs.bk
# Mojang-derived reference data must never leave this machine (PRD 4, 8)
/reference/
/fixtures/vanilla/
*.mca
# Reference worlds for `oxide-harness --reference` are server output, not ours to
# redistribute. Keep them outside the repo; these patterns are a second line of defence.
/reference-world*/
/world/
/world_nether/
/world_the_end/
level.dat*
*.jar
/plugin/build
/plugin/.gradle
.DS_Store
